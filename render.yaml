services:
  - type: web
    name: smart-telegram-bot
    runtime: python
    pythonVersion: 3.10
    buildCommand: pip install -r requirements.txt
    startCommand: python3 bot.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: BITLY_TOKEN
        sync: false
